<div class="bg-layer" aria-hidden="true">
  <div class="mesh-wrap">
    <div class="blob blob--1"></div>
    <div class="blob blob--2"></div>
    <div class="blob blob--3"></div>
    <div class="blob blob--4"></div>
    <div class="blob blob--5"></div>
  </div>
</div>

<style>
  .bg-layer {
    position: fixed;
    inset: 0;
    z-index: 0;
    background: var(--bg);
  }

  .mesh-wrap {
    position: absolute;
    inset: -20%;
    overflow: hidden;
    filter: blur(70px);
  }

  .blob { position: absolute; border-radius: 50%; }

  .blob--1 {
    width: 640px; height: 640px;
    background: radial-gradient(circle, rgba(37, 99, 235, 0.55) 0%, rgba(37, 99, 235, 0.1) 50%, transparent 72%);
    top: -10%; left: 6%;
  }
  .blob--2 {
    width: 580px; height: 580px;
    background: radial-gradient(circle, rgba(124, 58, 237, 0.5) 0%, rgba(124, 58, 237, 0.09) 50%, transparent 72%);
    top: 4%; right: -10%;
  }
  .blob--3 {
    width: 540px; height: 540px;
    background: radial-gradient(circle, rgba(6, 182, 212, 0.45) 0%, rgba(6, 182, 212, 0.08) 50%, transparent 72%);
    bottom: -14%; left: 30%;
  }
  .blob--4 {
    width: 480px; height: 480px;
    background: radial-gradient(circle, rgba(59, 130, 246, 0.4) 0%, rgba(59, 130, 246, 0.06) 50%, transparent 72%);
    top: 32%; left: 42%;
  }
  .blob--5 {
    width: 420px; height: 420px;
    background: radial-gradient(circle, rgba(168, 85, 247, 0.35) 0%, rgba(168, 85, 247, 0.06) 52%, transparent 70%);
    bottom: 6%; right: 18%;
  }

  /* Faint dot grid — a touch of texture */
  .bg-layer::before {
    content: '';
    position: absolute;
    inset: 0;
    background-image: radial-gradient(circle at 1px 1px, rgba(0, 0, 0, 0.02) 1px, transparent 0);
    background-size: 30px 30px;
    z-index: 1;
    pointer-events: none;
  }

  /* Five blobs, all different speeds/paths/phases → organic random drift that
     never obviously repeats. Negative delays desync them from the first frame. */
  @media (prefers-reduced-motion: no-preference) {
    .blob--1 { animation: drift1 21s ease-in-out infinite; animation-delay: -4s; }
    .blob--2 { animation: drift2 27s ease-in-out infinite; animation-delay: -11s; }
    .blob--3 { animation: drift3 24s ease-in-out infinite; animation-delay: -7s; }
    .blob--4 { animation: drift4 18s ease-in-out infinite; animation-delay: -14s; }
    .blob--5 { animation: drift5 30s ease-in-out infinite; animation-delay: -3s; }
  }

  @keyframes drift1 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(90px, 55px) scale(1.12); }
    50% { transform: translate(35px, 105px) scale(0.93); }
    75% { transform: translate(-60px, 50px) scale(1.09); }
  }
  @keyframes drift2 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(-75px, 65px) scale(1.1); }
    50% { transform: translate(-40px, -60px) scale(1.18); }
    75% { transform: translate(55px, 25px) scale(0.94); }
  }
  @keyframes drift3 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(65px, -50px) scale(1.1); }
    50% { transform: translate(-50px, -80px) scale(0.9); }
    75% { transform: translate(-70px, 40px) scale(1.1); }
  }
  @keyframes drift4 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    25% { transform: translate(-55px, -45px) scale(1.14); }
    50% { transform: translate(60px, -30px) scale(0.92); }
    75% { transform: translate(35px, 60px) scale(1.06); }
  }
  @keyframes drift5 {
    0%, 100% { transform: translate(0, 0) scale(1); }
    33% { transform: translate(-45px, 55px) scale(1.16); }
    66% { transform: translate(70px, -35px) scale(0.9); }
  }

  @media (max-width: 480px) {
    .blob--1 { width: 320px; height: 320px; }
    .blob--2 { width: 300px; height: 300px; }
    .blob--3 { width: 280px; height: 280px; }
    .blob--4 { width: 240px; height: 240px; }
    .blob--5 { width: 220px; height: 220px; }
    .mesh-wrap { filter: blur(55px); }
  }
</style>
